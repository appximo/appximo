# gh-pages — Appximo

- `/` — la página oficial (copia publicada de `site/` en main; para actualizarla: copiar `site/` acá y push).
- `/dev/bench/` — datos y chart del workflow Benchmark (github-action-benchmark los pushea automáticamente).

Rama con raíz limpia (init vacío). La gh-pages del repo de trabajo viejo NO se
republicó a propósito — su data.js arrastraba mensajes de commits de la
historia sin filtrar (HOUSEKEEPING-S1, 2026-08-05).
