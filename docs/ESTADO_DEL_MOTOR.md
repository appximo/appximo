# Estado del motor — qué tiene Appximo, en una página

Índice de **todo lo construido**, ordenado por área (no por fecha): qué es, en qué versión entró, dónde se ve, dónde se documenta y cómo se repite con `appximo drill`. Es la respuesta a «¿qué tiene mi motor?». El manual de operación en español es [MANUAL_OPERACION.md](MANUAL_OPERACION.md); la referencia en inglés, [CAPABILITIES.md](CAPABILITIES.md); los pendientes, [BACKLOG.md](BACKLOG.md).

**Cómo se mantiene.** A mano, con una red: el test `cmd/appximo/docs_index_test.go` lee este archivo y el manual y falla si nombran un subcomando de `appximo drill` que no existe, un `docs/*.md` que no existe, o una variable `APPXIMO_*` que el código no lee. No se genera solo porque su fuente sería el backlog (prosa por sesión, sin estructura) y no hay ninguna fuente estructurada de «lo que existe» aparte del propio código; lo que sí se puede automatizar es que **no se pudra** — y eso está automatizado. Cada fila se agrega en la sesión que construye la cosa, junto con su entrada DONE en el backlog (regla del handoff). «Versión» es el primer release público que lo lleva (`v0.1.13` es el último cortado; `main` = todavía sin release).

**Qué versión tiene qué (leído en GitHub el 2026-09-20).** Esta página describe `main`. El último release PUBLICADO es **v0.1.13 (2026-08-28)** y trae el motor completo — schema → API, RBAC, migraciones, archivos, autenticación, `/admin`, `/app`, Studio, `up`/`new`/`ai-generate` — pero **NO** trae nada del frente de automatización y voz: ni el ejecutor de workflows ni el binario `appximo-worker` entre sus archivos, ni el resumen (`/api/summary`), ni las preguntas y escrituras por voz (`/api/ask`), ni los `aliases`, ni el techo de gasto, ni el bot de Telegram, ni `appximo drill`. Las etiquetas v0.1.14–v0.1.16 existen en GitHub sin release publicado (CI rota a propósito, publicación pausada — DEC-2 del backlog) y también son anteriores a todo eso. Hoy esas capacidades se obtienen **compilando `main`** (`go build ./cmd/appximo ./cmd/appximo-worker`, o `scripts/build-engine.sh` + `build-worker.sh`) e instalando con `install.sh --binary=… --worker-binary=…`; cortar un release es decisión de Miguel, y ningún documento de este repo promete lo contrario.

| Área | Qué hay | Versión | Dónde se ve | Doc | Repetirlo |
|---|---|---|---|---|---|
| **Schema → API** | REST + GraphQL + OpenAPI compilados del `schema.json` al arrancar; tablas por tenant creadas al registrar; validación declarativa (422 con todos los campos); máquinas de estado; relaciones y embeds `?include=`; `?fields=` empujado al SQL; agregados; `/api/transaction` atómica; **rangos de tiempo con no-solapamiento** (`ranges` + `no_overlap`: `EXCLUDE` real, 409 que nombra la fila, `?filter[rango][overlaps]`, `GET /api/{res}/conflicts`) | v0.1.0 · `?fields=` v0.1.13 · rangos `main` (MOTOR-AGENDA-S1) | `/docs`, `/openapi.json`, `/graphql` | [AGENTS.md](../AGENTS.md), [SCHEMA_REFERENCE.md](SCHEMA_REFERENCE.md) | `appximo validate --json`, `appximo explain --lang es` |
| **Autoría** | Studio (`/editor`: ERD, roles, máquinas de estado, deploy con vista previa y aprobación de borrados, reinicio en un clic), `appximo up` / `new` / `ai-generate`, `spec` + `backend-spec` + `frontend-spec` + `backoffice-spec` + `quickstart` para agentes; **el generador declara los bloques operativos por señal de la descripción** — un recordatorio ⇒ workflow cron, «avisame cuando» ⇒ `events` + workflow por evento, una app a la que se le habla ⇒ `aliases`, un ciclo de vida ⇒ `pending` — y nada «por si acaso» (probado con tres apps: tareas con todo, citas solo el recordatorio, inventario nada) | v0.1.1 · bloques operativos `main` (AUTO-10) | `/editor` | [AUTHORING_JOURNEY.md](AUTHORING_JOURNEY.md), [SCHEMA_SPEC_LLM.md](SCHEMA_SPEC_LLM.md) | `appximo specs` |
| **Migraciones** | Diff contra la base viva, aditivas por defecto, borrados con aprobación enumerada, re-introspección tras aplicar (parcial = fallo), `renamed_from`, FKs reales, fan-out a N tenants reanudable, historial de versiones y rollback | v0.1.0 · historial v0.1.5 | `/admin#/history`, Studio → Deploy | [MENTAL_MODEL.md](MENTAL_MODEL.md), [MIGRATION_DIAG.md](MIGRATION_DIAG.md) | `appximo migrate --dry-run` |
| **Multi-tenant** | Un tenant por subdominio, un schema Postgres por tenant, el id = primer label del dominio; suspensión; borrado sin huérfanos | v0.1.0 | `/admin#/tenants` | [MENTAL_MODEL.md](MENTAL_MODEL.md) | `appximo tenant list` |
| **Autenticación** | Registro/login/refresh, reset y verificación por correo (outbox + worker), OAuth Google/GitHub/Microsoft, TOTP + códigos de respaldo, límite de intentos por identidad (`APPXIMO_AUTH_LOGIN_ATTEMPTS_PER_MINUTE`), super-admin de plataforma con MFA | v0.1.0 · perilla de login v0.1.10 | `/admin#/users`, `POST /auth/*` | [PRODUCTION.md §8](PRODUCTION.md) | — |
| **Autorización** | RBAC por rol y por recurso, condiciones de fila (`$user_id`), allowlist de campos, `routes` para rutas custom, `rbac.public` anónimo, la columna de identidad es del servidor también al actualizar (ADR-027) | v0.1.0 · ADR-027 v0.1.10 | 403/404 en la API | [audits/AUTHZ_WRITE_AUDIT_S1.md](audits/AUTHZ_WRITE_AUDIT_S1.md) | — |
| **Archivos** | Almacén por contenido, local o S3/R2/Spaces/MinIO, validación OWASP de subida, campo `file` con FK real y política `accept`/`max_bytes`, URL firmadas, `Ctx.ServeFile` | v0.1.0 | `/admin#/files`, `POST /api/files` | [FILES.md](FILES.md) | — |
| **Librería Go** | `appximo.New` + `Route` + `Ctx` (Query/Get/Insert/Update con RBAC, SafeGo, Enqueue, CreateUser, MintToken, ServeFile), estáticos embebidos con CSP, errores tipados, `BeforeStart` | v0.1.1 | — | [BACKEND_SPEC_LLM.md](BACKEND_SPEC_LLM.md) | `appximo backend-spec` |
| **Panel del dueño (`/app`)** | CRUD genérico desde `/openapi.json`: listas con paginación honesta y el tiempo del motor, tablero por máquina de estados, detalle con relaciones, editor JSON, vistas guardadas, CSV, acciones masivas; ES/EN, móvil, oscuro, tema por CSS, modo demo, barra de retorno | v0.1.11 · poder v0.1.13 | `https://<tenant>.<dominio>/app` | [BACKOFFICE_SPEC_LLM.md](BACKOFFICE_SPEC_LLM.md) | — |
| **Panel de administración (`/admin`)** | Inicio con **Salud ahora** (veredicto, backup, disco, problemas), Tenants, Datos, Usuarios, Archivos, Historial, Observabilidad, Recursos; **español/inglés** con toggle; línea de contexto por sección; pestañas en la URL; Problemas → Traza enlazados | v0.1.1 · ES + salud `main` | `/admin` | [MANUAL_OPERACION.md §2](MANUAL_OPERACION.md) | `appximo drill error` |
| **Observabilidad** | Trazas por request con etapas, `Server-Timing` en cada lectura, SLO con burn-rate multi-ventana, anomalías z-score, **un 500 explicado** (mensaje, sentencia fallida, usuario, rol, pila, grupos por huella, alerta a la primera ocurrencia), persistencia en `obs.db`, `/metrics` Prometheus | v0.1.1 · 500 explicado `main` | `/admin#/observability`, `/metrics`, `/debug/*` | [EXPLORE.md](EXPLORE.md), [MANUAL_OPERACION.md §2.1](MANUAL_OPERACION.md) | `appximo drill error` |
| **Auto-monitor (Módulo C)** | Colector de 4 capas fuera del camino caliente (runtime, cgroup+PSI, pool+consultas, capa 5 disco/backup) y **veredicto determinista** de 8 valores con evidencia; tablero vivo, ventana de carga, instantánea exportable; 21 gauges `appximo_selfmon_*` | v0.1.13 | `/admin#/resources` | [adr/ADR-030](adr/ADR-030-self-observability-and-deterministic-attribution.md), [BENCHMARKS §4c](BENCHMARKS.md) | `appximo drill load`, `drill saturate` |
| **Protecciones bajo carga** | Control de admisión por concurrencia (`APPXIMO_MAX_INFLIGHT`, 429 temprano), límite por tenant derivado del techo medido (`RATE_LIMIT_RPS` = 350 × vCPU), guardia de memoria (`APPXIMO_MEMORY_GUARD_MIN_MB`, 503 en escrituras), breaker hacia la base (20 fallos seguidos → 503 en < 0,2 s), timeout de consulta 5 s | admisión y límite derivado `main` · guardia v0.1.12 · breaker `main` | Log de arranque; `/admin#/resources` (429/503) | [BENCHMARKS §4e](BENCHMARKS.md), [MANUAL_OPERACION.md §3](MANUAL_OPERACION.md) | `drill saturate`, `drill chaos 5`, `drill chaos 6`, `drill chaos 10` |
| **Capacidad** | Laboratorio efímero en DigitalOcean con cinturón de seguridad (`tools/lab`), generador de modelo abierto con corrección de omisión coordinada y ajuste USL con puerta de confianza (`tools/capacity`), protocolo ABBA congelado | `main` | `tools/lab/README.md` | [BENCHMARKS §4d–4e](BENCHMARKS.md) | `lab up/sweep/report/down` |
| **Backup y restauración** | Un set = dump + archivos + secretos + manifiesto de conteos; timer nocturno del instalador; `pg_amcheck` en cada backup; copia fuera de la caja (scp/rclone) con secretos cifrados; `restore.sh` cronometrado y verificado (13,6 s medidos); alertas de backup viejo/fallido y disco | `main` | `/var/backups/<app>/`, `/admin#/resources` (Disco y backup), `/admin#/` | [PRODUCTION.md §4](PRODUCTION.md) | `appximo drill restore`, `drill chaos 4` |
| **Resiliencia** | Unidad que nunca se rinde (`RestartSec=2`, `StartLimitIntervalSec=0`), PostgreSQL con `Restart=on-failure` y checksums, diez experimentos de caos con hipótesis escrita antes | `main` | `systemctl show -p RestartUSec <app>` | [PRODUCTION.md §4.4](PRODUCTION.md), [MANUAL_OPERACION.md §6.4](MANUAL_OPERACION.md) | `appximo drill chaos 1..10` |
| **Instalación y despliegue** | `install.sh` (VPS vacío → HTTPS; verifica instalado == pedido; `--app` para varias apps; `--harden`), `deploy-update.sh` (swap atómico + rollback), `deploy-app.sh` (backup → swap → **verificación desde afuera** → rollback automático → audit), `fleet-audit.sh` (qué falta), puesta al día de una caja vieja | instalador v0.1.1 · verificación v0.1.12 · deploy-app `main` | La caja | [PRODUCTION.md §1–3, §4.5b](PRODUCTION.md), [MANUAL_OPERACION.md §5](MANUAL_OPERACION.md) | `appximo drill audit` |
| **Simulacros** | `appximo drill list/error/load/saturate/probe/chaos/restore/audit/ask/voice/spend` — cada uno dice qué provoca, qué debería pasar y dónde mirarlo; se niega contra producción sin `--production`; restaura lo que rompe; los tres de voz prueban el canal de punta a punta (`voice` en un tenant efímero que crea y borra) | `main` | La terminal + el panel | [MANUAL_OPERACION.md §6](MANUAL_OPERACION.md) | `appximo drill list` |
| **Flota** | N apps en un servidor (`fleet run` multi-proceso o `fleet serve` en un proceso, hot-swap por app), consola `/fleet` | v0.1.0 | `/fleet` | [FLEET.md](FLEET.md) | `appximo fleet status` |
| **Trabajos en segundo plano** | **Recordatorios por fila** (`workflows` con trigger `time`: «15 min antes de cada cita», barridos por el líder y reclamados exactamente una vez por fila e instante en `public.workflow_reminders`, en la misma transacción que el `enqueue`; el tema `message.telegram` manda el texto al chat — MOTOR-AGENDA-S1); outbox transaccional por recurso (`events`), worker aparte con LISTEN/NOTIFY y **reclamo acotado por tema** (nada ackea un tema sin consumidor; un fallado guarda su error en `last_error`), consumidores XLSX y correo, JWT de servicio acotado; **cola observable**: edad del pendiente más viejo + fallados en `/metrics`, `GET /admin/outbox` y alertas; webhooks agotados a `webhook.dead` | v0.1.0 · trampas cerradas `main` | `GET /admin/outbox`, `public.outbox` | [PERSISTENCE.md](PERSISTENCE.md), [PRODUCTION.md §8b](PRODUCTION.md) | `scripts/worker-e2e.sh` |
| **Workflows** | Pipelines declarativos del schema (trigger evento/cron → condición/update/create/webhook/enqueue), ejecutados por `appximo-worker` (modo `auto`, instalado por `install.sh`): expresiones expr-lang compiladas al validar, líder por `pg_try_advisory_lock`, solape declarado, DST por escrito, corridas en `public.workflow_runs` | `main` (ADR-031) | `GET /admin/workflows`, `appximo_workflow_*` | [SCHEMA_REFERENCE.md §1.4](SCHEMA_REFERENCE.md), [adr/ADR-031](adr/ADR-031-workflows-executor.md), ejemplo canónico [examples/model-lab/agenda-voz.json](../examples/model-lab/agenda-voz.json) | `appximo validate` sobre un schema con `workflows`; `GET /admin/workflows` (última y próxima corrida) |
| **Alertas y resumen por Telegram** | El alertador manda a un chat de Telegram (backup viejo/fallido, disco, outbox varado, workflow atrasado, gasto del modelo); el bot RECIBE `resumen` / `estado` / `ayuda` / `gasto` desde el chat autorizado; **el resumen del día** (`GET /api/summary`, `?format=png` la imagen renderizada por el motor) en las palabras del dueño: creados hoy, movidos hoy, lo que ESPERA (los estados `state_machine.pending`), el delta contra ayer, «igual que ayer» plegado; un cron `workflow` que encola `summary.telegram` lo manda cada mañana y CALLA cuando no hay novedad (`summary.notify`, `quiet_days`) | `main` (ADR-032/034) | El chat de Telegram; `GET /api/summary` | [PRODUCTION.md §4.6d](PRODUCTION.md), [MANUAL_OPERACION.md §3c](MANUAL_OPERACION.md), [adr/ADR-032](adr/ADR-032-digest-as-image-declared-attention.md), [adr/ADR-034](adr/ADR-034-digest-delta-and-silence.md) | `resumen` en el chat; `curl /api/summary` con un token del rol |
| **Preguntas por voz** | `POST /api/ask {"q": "…"}`: una frase en las palabras del dueño → un PLAN cerrado sobre UN recurso (contar/listar/sumar/… con filtros, periodo, nombres propios resueltos contra las filas con fonética española) ejecutado con el RBAC del rol; **el modelo es el último recurso**: parser determinístico del schema (US$ 0, ms) → caché de planes → modelo (Haiku, ≈ US$ 0,003, ~1 s); `⚙︎` con quién respondió, latencia y costo; historial redactado (`public.ask_history`); Siri por atajo | `main` (ADR-033/035/036) | El chat de Telegram; Siri; `POST /api/ask` | [PRODUCTION.md §4.6e](PRODUCTION.md), [MANUAL_OPERACION.md §3d](MANUAL_OPERACION.md), [adr/ADR-033](adr/ADR-033-voice-read-questions.md), [adr/ADR-035](adr/ADR-035-questions-without-a-model.md), [adr/ADR-036](adr/ADR-036-question-traceability.md) | `appximo drill ask --app=<app> --tenant=<t> "cuántas órdenes hay hoy"` |
| **Escrituras por voz** | Crear y cambiar por voz — **nunca borrar** (rechazado sin llamar al modelo) — con una confirmación que lista CADA valor tal como quedará (nombres resueltos a la fila, fechas en la zona de la app, un campo obligatorio que faltó se PREGUNTA); el sí es una lista cerrada; la escritura confirmada pasa por los cores de `/api/transaction` (RBAC, validadores, hooks, máquina de estados, outbox); una transición de estado la resuelve el parser solo (US$ 0); botones Sí/No en Telegram; `APPXIMO_ASK_WRITES=off` deja todo en solo lectura | `main` (ADR-037) | El chat; `POST /api/ask` (`kind: confirm` → `written`) | [MANUAL_OPERACION.md §3e](MANUAL_OPERACION.md), [adr/ADR-037](adr/ADR-037-voice-writes-with-confirmation.md) | `appximo drill voice --app=<app>` |
| **Sinónimos (`aliases`)** | El schema declara cómo el dueño NOMBRA cada recurso y cada estado (`"aliases": ["pedidos","ventas"]`, `"aliases": {"pagada": ["cobrada"]}`), validados únicos al cargar con las mismas formas que usa el parser; el parser, el modelo, Studio (vista Code), `explain` y `spec` los conocen; medido en el uso real: la parte del parser subió de 50 % a 75 % | `main` (ADR-038) | `appximo explain`; la respuesta del bot | [SCHEMA_REFERENCE.md §2.6/§4.11](SCHEMA_REFERENCE.md), [adr/ADR-038](adr/ADR-038-declared-aliases-and-what-a-question-costs.md) | `appximo drill ask` con un alias en la pregunta → `source=parser` |
| **Gasto del modelo** | Techo por tenant (`APPXIMO_ASK_PER_MINUTE` 6, `APPXIMO_ASK_DAILY_USD` 0,50; por usuario `APPXIMO_ASK_DAILY_USD_PER_USER`), alerta al 80 % y al techo, **gasto útil vs desperdiciado** (una pregunta que no fue pregunta no se cobra), la tarjeta `gasto` (imagen + texto), `GET /api/ask/spend`, `GET /admin/ask?tenant=`, `appximo_ask_*` en `/metrics` | `main` (ADR-035/036/038) | `gasto` en el chat; `/admin/ask` | [PRODUCTION.md §4.6e–f](PRODUCTION.md), [adr/ADR-038](adr/ADR-038-declared-aliases-and-what-a-question-costs.md) | `appximo drill spend --app=<app> --tenant=<t>` |
| **Calidad y gates** | Lane unitaria (`make test`), lane completa con Docker, `binary-diff-gate.sh` (171 casos entre dos binarios), `bench-protocol.sh` + ABBA, `verify-production/` (suite del stack entero), certificación con fecha | — | CI | [BENCHMARKS.md](BENCHMARKS.md), [CERTIFICATION_2026-08-01.md](CERTIFICATION_2026-08-01.md) | `scripts/binary-diff-gate.sh` |
| **Centro de mando** | Una app hecha CON Appximo, en su propia caja y región, que junta toda la operación: salud/veredicto/backup/disco/problemas por app, `fleet-audit.sh` por caja, pendientes del backlog y decisiones leídos de la fuente, inventario editable (servidores, apps, dominios, clientes, contactos, dónde está cada cosa — sin claves), acciones sobre los scripts probados (actualizar, simulacro, auditar, restaurar, migrar) con las tres formas (lo hago yo / lo hacés vos + Verificar / bloqueado) y «Pedir ayuda» | `main` (repo interno `centro-mando/`) | La caja del panel | [MANUAL_OPERACION.md §9](MANUAL_OPERACION.md) | `deploy-app.sh` sobre sí mismo |
| **Una app REAL sobre todo lo anterior (agenda del dueño)** | La agenda de Miguel (APP-AGENDA-S2: habla por Telegram COMO el dueño — `APPXIMO_TELEGRAM_SUMMARY_USER_ID` —, el parte abre con «Hoy en agenda», «tengo que…» anota una tarea) — tareas/compromisos/registros/personas/áreas/etiquetas, `ranges` con `no_overlap` por dueño, hook estimado-vs-real, 5 workflows (partes 7:00 y 19:00, aviso 15 min, urgente, espejo), RBAC por fila, Siri con token acotado (`--paths /api/ask,/api/summary`), tercera app del 58 con base y worker propios, copia cifrada fuera de la caja restaurada en OTRA caja; el schema declara solo los `events` que un workflow consume | `main` (APP-AGENDA-S1, 2026-09-22) | https://agenda.162-243-64-58.sslip.io (`/app`, `/admin`) · centro de mando `apps/agenda` | [MANUAL §3g](MANUAL_OPERACION.md#3g-la-agenda-del-dueño--la-primera-app-real-sobre-todo-lo-anterior-app-agenda-s1) · evidencia interna `APP-AGENDA-S1/` | `install.sh --app=<x> --harden --worker-binary=…` + `drill restore` + el set copiado en un `postgres:18` efímero |
| **Lo que no hace** | Una caja es una caja; RPO = cadencia del timer; checksums solo al leer; degradar ≠ aguantar; residuos GraphQL; no cero-downtime; sin importador masivo; mensajes del motor en inglés | — | — | [MANUAL_OPERACION.md §7](MANUAL_OPERACION.md), [GUIDE.md §9](GUIDE.md) | — |
